// print is used to display anythings into the terminal
// the code will execute in sequence (first line than second and so on)
// all dart line must end with ;
// main is where dart compiler will start execute our code
void main() {
  print("Hello Word");
  print(3);
  print(true);
  print([4,3,1]);  
}

